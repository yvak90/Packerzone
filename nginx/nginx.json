{
    "variables" : {
        "aws_access_key" : "",
        "aws_secret_key" : "",
        "aws_source_ami" : "ami-0cda377a1b884a1bc"

    },
    "builders" : [
        {
            "type" : "amazon-ebs",
            "ami_name" : "my-nginx-{{isotime | clean_resource_name}}",
            "ami_description" : "AMI created with packer",
            "ami_regions" : ["ap-south-1","us-west-2"],
            "access_key" : "{{user `aws_access_key`}}",
            "secret_key" : "{{user `aws_secret_key`}}",
            "instance_type" : "t2.micro",
            "source_ami" : "{{user `aws_source_ami`}}",
            "ssh_username": "ubuntu"
        }
    ],
    "provisioners" : [
        {
            "type" : "shell",
            "inline" : [
                "sudo apt-get update",
                "sudo apt install nginx -y"
            ]
        }
    ]
}